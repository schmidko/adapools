PORT=5056
MONGO_HOST=host.docker.internal
MONGO_PORT=27017
MONGO_DB=adablox
MONGO_USER=
MONGO_PASSWORD=
MONGO_AUTH_SOURCE=admin
MONGODB_URI=
CORS_ORIGIN=true
ADAPOOLS_BLOCK_CACHE_LIMIT=240
ADAPOOLS_WS_POLL_INTERVAL_MS=3000
POOL_ADS_ENABLED=false
# Required only when POOL_ADS_ENABLED=true. Never expose these values to the browser.
POOL_ADS_PAYMENT_ADDRESS=
# Internal db-sync connection for server-side payment verification.
ADAPOOLS_DATABASE_URL=
